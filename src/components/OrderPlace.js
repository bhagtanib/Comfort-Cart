import React from "react";
import "../styles/OrderPlace.css";
const OrderPlace = () => {
  return (
    <div class="order-info">
      Unfortunately we are not taking orders right now 😔
    </div>
  );
};

export default OrderPlace;
